if (<%=@result==1%>)
 {
 	alert("Sapname Cannot blacnk!");
 } 
else
{
	var oTable=$("#checkmen").dataTable();
	var ntd = $("#<%=@package.id%>.package_edit");
	ntd.text("<%=@person.firstname+' '+@person.lastname%>");
	var ntr = ntd.parents("tr")[0];
	oTable.fnClose(ntr);
};