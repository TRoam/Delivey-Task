$(document).ready(function() {

    var oTable = $("#checkmen_result").dataTable();
    var nTr= $("#<%= @checkman.id%>").parents('tr')[0];
    oTable.fnOpen( nTr, "<%= j render :partial=>'preson_edit'%>", 'details' );
} );