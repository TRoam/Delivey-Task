$("#object_edit_area").append("<%= j render :partial => 'form'%>")
var divheight = $("#object_edit_area").height();
var windowheight = $(window).height();
if(divheight > windowheight){
	$("#object_edit_area").find("div:first").remove();
	return;
}